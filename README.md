# Sorting algorithms

This project contains sorting algorithms such as **insertion sort**, **bubble sort**, **selection sort**, **merge-sort** and **quick-sort**. It also contains tests for all these algorithms. The code is written in TypeScript and an auto-generated JavaScript version.